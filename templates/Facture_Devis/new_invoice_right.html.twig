{% block title %}{% endblock %}
{% block head %}{% endblock %}
    {% block stylesheets %}
        <link rel="stylesheet" type="text/css" href="{{ asset('build/CSS/Facture-devis/facture-devis.css') }}">
    {% endblock %}
{% block content %}
    {{ form_start(form2, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="right_block">
        <div class="right-one scroll-two">
            <table class="table-right">
                <thead>
                    <th>Compte charge</th>
                    <th>Taux Tva</th>
                    <th>QTT</th>
                    <th>Tva</th>
                    <th>Montant</th>
                </thead>
                <tbody>

                </tbody>
            </table>
        </div>
        <div class="right-two">
            <h5 class="title_left" style="text-align: center;margin-top: 10px ">Moyens de Paiment</h5>



            <div class="form-error">
            </div>
            <div class="scroll">
            <div class="funkyradio">
            {% for children in form2.record_id %}
            <div class="funkyradio-warning col-md-8">{{ form_widget(children) }} {{ form_label(children) }}</div>
            {% endfor %}
            </div>
            </div>

            {#{{ form_widget(form2.record_id, {'attr': {'class': 'input_amount_qtt form-control'}} ) }}#}
        </div>

        <div class="right-tree">
            <p>Client</p>
            {{ form_widget(form2.person_id, {'attr': {'class': 'input_amount_qtt form-control'}} ) }}
        </div>


        <div class="right-four">
            {{ form_label(form2.invoice_date, null, { 'label': 'Date de la Facture :','label_attr': {'class': 'title_left col-md-5 control-label'} }) }}
            <div class="col-md-5">
                <div class ="input-group">
                                    <span class="input-group-addon"> <i
                                                class="fa fa-calendar"> </i></span>
                    {{ form_widget(form2.invoice_date, {'attr': {'class': 'js-datepicker form-control'}} ) }}
                </div>

            </div>
            <button class="confirmation_Def_action_button">Je Valide Ma Facture Définitiveme</button>
        </div>
    </div>

    {{ form_end(form2) }}
{% endblock %}


